#### stage1训练
### 只进行1epoch训练：
    {'train_runtime': 26.5838, 'train_samples_per_second': 26.294, 'train_steps_per_second': 0.188, 'train_loss': 6.329545783996582, 'epoch': 0.91}
    {'loss': 4.2956, 'grad_norm': 4.972905158996582, 'learning_rate': 0.0, 'epoch': 0.91} 

### 进行3epochs训练：
    ## 第一次：
        Loss 曲线
        Epoch 1:  8.38 → 4.54   (快速收敛)
        Epoch 2:  5.88 → 3.74   (epoch 边界震荡后继续下降)
        Epoch 3:  5.02 → 3.57   (趋于平稳)
        收敛情况
        - Grad norm 从 68.7 降到 2.2，说明梯度已非常稳定
        - Epoch 2→3 的 loss 下降幅度明显变小（3.74→3.57），接近收敛
        - 每个 epoch 边界处有轻微 loss spike（数据 shuffle 导致），正常现象
        与之前对比
        结论
        3 epochs 比 1 epoch 好 17%，grad_norm 稳定在 2.2，基本收敛。可以进 Stage 2 了。
        不过刚才发现权重保存时形状被展平了（[3145728] vs [4096,768]）
    ## 第二次：
        Loss 曲线
        Epoch 1:  7.45 → 3.99   (快速下降)
        Epoch 2:  5.26 → 3.41   (epoch边界spike后继续下降)
        Epoch 3:  4.63 → 3.32   (平稳收敛)
        收敛情况
        grad_norm 收敛到 1.4 意味着梯度方向已经非常稳定，loss 下降幅度也非常小，说明已经接近收敛
    ## 最终指标：
        loss: 3.32，进入下一个阶段

#### stage2训练
