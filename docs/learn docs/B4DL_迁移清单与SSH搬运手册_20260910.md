# B4DL 项目迁移清单与 SSH 搬运手册

**日期**：2026-09-10
**源机**：AutoDL RTX 5090（仓库 `/root/autodl-tmp/wql/mmb4dl`，环境 `wqlc`）
**目标**：新租云服务器（目录布局见 §2）
**依据**：本文所有体积均为源机 `du` 实测；"是否需要"均经代码检索验证，非估计

---

## 0. 结论速览

代码已与 `origin/main` 同步（无未推送 commit），因此 **代码用 `git clone` 拿，只搬 5 个数据包（约 22G）+ nuScenes 关键帧子集（31G）+ conda 环境（8.4G）**。

**不需要搬**：`sweeps/`（338G，全代码零引用）、其余 5 路相机样本、`lidarseg`、checkpoint 里的优化器状态（约 5G）、roberta-large 的 4 种重复格式（13.6G）、`.claude/` 会话记录（601M）、`.ssh/`（含私钥，禁止外传）。

**搬运方式**：首选「源机 → 新机 直连 rsync/tar 流式」，不经本地盘；小文件（28,130 个 `.npy`）**必须先打 tar**。

---

## 1. 容量汇总

| 批次 | 内容 | 体积 |
|---|---|---|
| A | 代码 + 基座模型 + 检查点 + 编码器 + 特征 + 数据 JSON | ~22G |
| A-env | conda 环境 `wqlc` | 8.4G |
| B | nuScenes 关键帧子集（**仅重提特征时**需要） | 31G |
| C | 可选：训练日志 / 评测产物 / 备份包 | ~3G |
| **合计** | 不含 C | **~61G** |

新机建议：**数据盘 ≥200G**（需容纳训练工作区：merged 完整 7B 模型约 14G、`save_total_limit 3` 的滚动 checkpoint 约 6G；历史上未清理时 checkpoints 峰值曾达 145G），**系统盘 ≥30G**（若 conda 环境装系统盘则 50G+）。

---

## 2. 目标机目录布局

照抄源机路径，可完全避免改写代码里的绝对路径：

| 用途 | 路径 |
|---|---|
| 仓库 | `/root/autodl-tmp/wql/mmb4dl` |
| nuScenes | `/root/autodl-tmp/Datasets/nuScenes` |
| conda 环境 | `/root/autodl-tmp/.conda-stuff/envs/wqlc`（直接搬环境时**路径必须一致**） |
| 中转包暂存 | `/root/autodl-tmp/transfer`（验收后可删） |

> 非 AutoDL 机器若无 `/root/autodl-tmp`，用同构前缀（如 `/data/wql/mmb4dl` + `/data/Datasets/nuScenes`），再按 §6.3 改写绝对路径。

---

## 3. 搬运清单

### 批次 A：必需（~22G）

| # | 源（相对仓库根，除注明外） | 体积 | 目标 | 说明 |
|---|---|---|---|---|
| A1 | `git clone git@github.com:Dani3lWang/remote4b4dl.git` | 106M（跟踪文件） | 仓库根 | 已同步，无未推送 commit |
| A2 | `mllm/base_model/vicuna-v1-5-7b` | 13G | 同名 | Vicuna-7B 基座，训练/评测都要 |
| A3 | `mllm/checkpoints/` | 1.7G | 同名 | stage1（`mm_projector.bin`，评测重建必需）+ B0/B3/B4a 三个 adapter（各 556M） |
| A4 | `encoders/lidarclip/` 代码与权重：`lidarclip/`（含旧 ONCE 的 `checkpoint/vit_l_14.ckpt` 987M）、`sst/`（7.4M） | ~1G | 同名 | `lidarclip/model/` 含几何配置 `sst_encoder_only_config.py` 与兼容层 `_mmdet3d_compat.py`；`sst/` 是捆绑的 mmdet3d 0.15.0 源码，**必须随行**（靠 `sys.path` 引入）。旧 ONCE ckpt 若确定不再用可剔除 |
| A5 | `encoders/lidarclip/ckpt_anneal/lidarclip_mm/last.ckpt` | 1.03G | 同名 | **定稿编码器**（val_mse_probe：MSE 0.0992，优于基线 `ckpt_nuscenes` 的 0.1061）。其中 `lidar_encoder.*` 仅 8.46M 参数 / 34MB，其余是提取时**不读取**的 `clip.*`（427.6M 参数）+ 优化器状态 |
| A6 | `encoders/lidarclip/annotations/{scene,sequence}_metadata.json` | 73M | 同名 | 提特征与 per-sequence 评测必需 |
| A7 | `encoders/lidarclip/b4dl/{stage1_features, stage1_features_sample, stage2_features}` | 333M | 同名 | 29,862 + 28,130 + 850 个 `.npy`；**小文件多，必须单独打 tar** |
| A8 | `encoders/lidarclip/pretrained/ViT-L-14.pt` | 922M | 同名 | OpenAI CLIP 图像塔（提特征时 `clip.load` 用）；也可在新机重新下载 |
| A9 | `mllm/b4dl_dataset/` | **501M** | 同名 | 2026-09-10 已清理（1.5G→501M，删除 `.bak*` 与全部被取代变体）：官方 QA + B3 训练数据 + 评测元数据共 15 个文件，清单与再生命令见 `B4DL_b4dl_dataset数据清理记录_20260910.md`。其中 B4a 过采样文件（132M）因 `_vram_smoke_bs8` 待跑作业暂留，作业结束后可删 |
| A10 | `models/roberta-large/` 仅 6 个文件：`config.json`、`model.safetensors`、`vocab.json`、`merges.txt`、`tokenizer.json`、`tokenizer_config.json` | 1.5G | 同名 | BERTScore 打分用；`flax_model.msgpack`/`model.onnx`/`tf_model.h5`/`pytorch_model.bin` 共 13.6G 全不搬 |
| A11 | `dataset/` | 384M | 同名 | HF B4DL 数据集 + LiDAR-LLM caption（追溯用） |
| A12 | `datageneration/data/` | 73M | 同名 | 已生成的场景描述 JSON |
| A13 | `mllm/env_wqlc_pip_freeze_20260910.txt`、`mllm/_vram_smoke_bs8/` | <100K | 同名 | **未跟踪文件**，须单独搬（环境重建清单在这里） |

### 批次 B：nuScenes 关键帧子集（31G，仅当要重提特征）

| 源（绝对路径） | 体积 | 说明 |
|---|---|---|
| `/root/autodl-tmp/Datasets/nuScenes/v1.0-trainval/` | 2.5G | JSON 元数据表（sample / sample_data / ego_pose 等） |
| `/root/autodl-tmp/Datasets/nuScenes/samples/LIDAR_TOP/` | 23G | 关键帧点云，特征的真实来源 |
| `/root/autodl-tmp/Datasets/nuScenes/samples/CAM_BACK_RIGHT/` | 5.1G | loader 在 `__getitem__` 会 `Image.open` 这一路图；内容不参与特征，但缺文件直接报错 |
| `/root/autodl-tmp/Datasets/nuScenes/maps/` | 5.6M | devkit 附带 |

**不需要**：`sweeps/`（338G）、`samples/` 其余 5 路相机（30G）、`samples/RADAR_*`（1.1G）、`nuScenes-lidarseg-all-v1.0/`（1.2G）、`dataset_info.list`（407M）、`test/`、`train/`、`trainval/`——`datageneration/`、`mllm/` 与提特征 loader 均不读这些（`sweeps` 在代码中零引用；提特征只遍历关键帧 `sample`）。

### 批次 C：可选（~3G，均**不在** git 跟踪范围内）

- 离线 wandb 记录：`mllm/wandb/`（137M）、`wandb/`
- 评测产物：`mllm/eval_results/`（8 次评测的 predictions+metrics，约 90M）
- `backups/`（3 个 tar 共 2.3G：B3 最终产物、seqv3 备份、全链 checkpoint 备份）

> 训练日志（`mllm/training_logs/`、`training_logs/`、`encoders/lidarclip/logs/`，含 `val_mse_probe.csv`）与 `mllm/evaluation/` 下的预测 JSON **已被 git 跟踪**，`git clone` 即自带，无需搬运。

### 明确不要搬

| 项 | 体积 | 原因 |
|---|---|---|
| `.ssh/`（`id_ed25519_b4dl`、`.ssh_project`） | 16K | **含私钥**，已 gitignore；换机器时排除 |
| `.claude/`（含 `worktrees/`） | 601M | 会话记录，与训练无关 |
| `.git/` | 1.5G | 直接 clone 更干净 |
| `encoders/lidarclip/ckpt_nuscenes/` | 3.9G | 退火前基线，4 份同规格副本；只需 A5 一份 |
| `encoders/lidarclip/ckpt_anneal/` 其余 3 份 | 3.1G | 同上（`epoch=1/2-step=*` 副本） |
| `encoders/lidarclip/mmdetection3d/` | 43M | mmdet3d 1.4.0 参考克隆；实际导入的是 `sst/`（0.15.0），代码路径不依赖它 |
| `encoders/lidarclip/{b4dl/*_once, wandb/, __pycache__}` | 353M+ | ONCE 旧编码器特征，非现行代次 |

---

## 4. 打包（源机执行）

```bash
mkdir -p /root/autodl-tmp/transfer
cd /root/autodl-tmp/wql/mmb4dl

# A2 基座模型（13G，最大的一包）
tar -cf /root/autodl-tmp/transfer/A2_base_model.tar mllm/base_model

# A3/A4/A5/A6/A9/A12/A13 合并一包（约 6.4G）
tar -cf /root/autodl-tmp/transfer/A3_ckpt_encoder_data.tar \
    --exclude='mllm/b4dl_dataset/*.bak*' \
    mllm/checkpoints mllm/b4dl_dataset datageneration/data \
    mllm/env_wqlc_pip_freeze_20260910.txt mllm/_vram_smoke_bs8 \
    encoders/lidarclip/lidarclip encoders/lidarclip/sst \
    encoders/lidarclip/pretrained/ViT-L-14.pt \
    encoders/lidarclip/annotations \
    encoders/lidarclip/ckpt_anneal/lidarclip_mm/last.ckpt

# A7 特征（小文件多，单独打；解包要 -C encoders/lidarclip/b4dl）
tar -cf /root/autodl-tmp/transfer/A7_features.tar -C encoders/lidarclip/b4dl \
    stage1_features stage1_features_sample stage2_features

# A10 roberta 精简版（解包要 -C models/roberta-large）
tar -cf /root/autodl-tmp/transfer/A10_roberta.tar -C models/roberta-large \
    config.json model.safetensors vocab.json merges.txt tokenizer.json tokenizer_config.json

# A11 + A14（可选：数据集描述 + 离线 wandb + 评测产物 + 备份包）
tar -cf /root/autodl-tmp/transfer/A11_dataset.tar dataset
tar -cf /root/autodl-tmp/transfer/A14_logs_eval.tar \
    mllm/eval_results mllm/wandb wandb backups

# B1 nuScenes 关键帧子集（31G；内容已是二进制，不加 -z）
tar -cf /root/autodl-tmp/transfer/B1_nuscenes_keyframes.tar \
    -C /root/autodl-tmp/Datasets/nuScenes \
    v1.0-trainval samples/LIDAR_TOP samples/CAM_BACK_RIGHT maps

# A16 conda 环境（8.4G，仅"直接搬环境"路线需要）
tar -cf /root/autodl-tmp/transfer/A16_conda_wqlc.tar \
    -C /root/autodl-tmp/.conda-stuff/envs wqlc

# 校验清单
cd /root/autodl-tmp/transfer && sha256sum *.tar > SHA256SUMS.txt && ls -lh
```

> `tar` 默认不压缩是刻意选择：模型权重、`.pcd.bin`、`.jpg`、`.npy` 都已是二进制/近似随机数据，`-z` 只会白烧 CPU。若要传代码或 JSON 包，可单独加 `-z`。

---

## 5. SSH 搬运方法

### 5.1 前置：密钥与连通性（源机执行一次）

```bash
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_migrate -N "" -C "b4dl-migrate"
ssh-copy-id -i ~/.ssh/id_ed25519_migrate.pub -p <新机端口> root@<新机地址>
ssh -p <新机端口> root@<新机地址> 'echo ok'          # 验证免密连通
```

AutoDL 的地址与端口在控制台「登录指令」里，形如 `ssh -p 12345 root@region-xx.autodl.com`。

### 5.2 方法一：源机 → 新机 直连（推荐，不经本地盘）

```bash
tmux new -s migrate                                   # 长任务防断线
ssh -p <新机端口> root@<新机地址> 'mkdir -p /root/autodl-tmp/transfer'
cd /root/autodl-tmp/transfer

rsync -avP --partial --append-verify -e 'ssh -p <新机端口>' \
    A2_base_model.tar A3_ckpt_encoder_data.tar A7_features.tar \
    A10_roberta.tar A11_dataset.tar A14_logs_eval.tar B1_nuscenes_keyframes.tar \
    root@<新机地址>:/root/autodl-tmp/transfer/
```

- `-P` 显示进度，`--partial --append-verify` 支持断点续传（`scp` 没有断点续传，大包别用 `scp`）
- 大二进制包不要加 `-z`；跨地域带宽小时，只有代码/JSON 包值得压缩
- 本地临时盘不够时，可以边打包边流式传输，不落中转包：

```bash
tar -cf - -C /root/autodl-tmp/wql/mmb4dl mllm/base_model \
  | ssh -p <新机端口> root@<新机地址> \
    'mkdir -p /root/autodl-tmp/wql/mmb4dl/mllm && tar -C /root/autodl-tmp/wql/mmb4dl/mllm -xf -'
```

带宽参考：13G 的基座模型包，20MB/s 约 11 分钟，5MB/s 约 45 分钟。

### 5.3 方法二：经本地中转（Windows 本地仓库场景 / 两台机器互不可达时）

本机 `~/.ssh/config` 配两个 Host，之后命令只需写 `old` / `new`：

```
Host old
  HostName <老机网关>
  Port <老机端口>
  User root
Host new
  HostName <新机网关>
  Port <新机端口>
  User root
```

两段式传输（WSL 或装了 rsync 的 Git Bash；Windows 自带 Git Bash 通常没有 rsync）：

```bash
rsync -avP old:/root/autodl-tmp/transfer/ ./transfer/     # 第一段：老机 → 本地
rsync -avP ./transfer/ new:/root/autodl-tmp/transfer/     # 第二段：本地 → 新机
```

纯 `scp` 版本（无断点续传，适合小包）：

```bash
scp -P <老机端口> -r root@<老机地址>:/root/autodl-tmp/transfer ./transfer
scp -P <新机端口> -r ./transfer/* root@<新机地址>:/root/autodl-tmp/transfer/
```

图形化替代：WinSCP / MobaXterm 各开一个会话，两栏互拖。本地盘紧张时按包分批中转，不要一次性全拉。

### 5.4 方法三：两台都是 AutoDL 时

- **文件存储**：控制台「文件存储」支持多实例同时挂载，写一次两端可见，适合 20~30G 级别
- **直连试探**：把源机公钥追加到新机 `~/.ssh/authorized_keys`，在源机执行 `ssh -p <新机端口> root@<新机网关>`；能否直连取决于网关策略，先试探再选路线
- 也可以走控制台的「文件管理 → 上传/下载」做 10G 以下的小包

### 5.5 传完立即校验

```bash
# 新机执行
cd /root/autodl-tmp/transfer && sha256sum -c SHA256SUMS.txt
```

---

## 6. 新机落地

### 6.1 代码

```bash
mkdir -p /root/autodl-tmp/wql && cd /root/autodl-tmp/wql
git clone git@github.com:Dani3lWang/remote4b4dl.git mmb4dl
cd mmb4dl && git log --oneline -3          # 期望 HEAD = 59fed9a
```

clone 后做一次代码完整性自检（确认关键代码都在跟踪范围内）：

```bash
git ls-files encoders/lidarclip/sst | wc -l    # 期望 454（捆绑的 mmdet3d 0.15.0 源码）
git ls-files mllm/vtimellm mllm/scripts datageneration | wc -l   # 期望 76
ls encoders/lidarclip/extract_pc_features_sample_token.py encoders/lidarclip/train.py \
   encoders/lidarclip/val_mse_probe.py encoders/lidarclip/run_anneal_chain.sh
```

> `extract_pc_features.py` 虽然被写进 `.gitignore`，但它已被强制跟踪（`git ls-files` 可查），clone 会带上——不要被 `.gitignore` 的字面意思误导而去手工补拷。

GitHub 凭据：源机用的是 `/root/.ssh/id_ed25519_github`（在 `~/.ssh/config` 的 `github.com` 段指定）。新机建议新生成一把 key，作为 deploy key 加到 `Dani3lWang/remote4b4dl`（需要 push 就勾选 write），比复制旧私钥更干净。

### 6.2 解包（注意 `-C`，包内是相对路径）

```bash
cd /root/autodl-tmp/wql/mmb4dl
tar -xf /root/autodl-tmp/transfer/A2_base_model.tar
tar -xf /root/autodl-tmp/transfer/A3_ckpt_encoder_data.tar
tar -xf /root/autodl-tmp/transfer/A11_dataset.tar
tar -xf /root/autodl-tmp/transfer/A14_logs_eval.tar
mkdir -p encoders/lidarclip/b4dl models/roberta-large
tar -xf /root/autodl-tmp/transfer/A7_features.tar -C encoders/lidarclip/b4dl
tar -xf /root/autodl-tmp/transfer/A10_roberta.tar -C models/roberta-large
mkdir -p /root/autodl-tmp/Datasets/nuScenes
tar -xf /root/autodl-tmp/transfer/B1_nuscenes_keyframes.tar -C /root/autodl-tmp/Datasets/nuScenes
```

### 6.3 绝对路径核对（仅当前缀与源机不同才需要）

写死了 `/root/autodl-tmp` 的文件（`grep` 实测）：`encoders/lidarclip/lidarclip/weight.py`、`encoders/lidarclip/smoke_train.py`、`validate_encoder_fit.py`、`validate_scatter.py`、`val_mse_probe.py`（默认值）、`extract_pc_features.py` 与 `extract_pc_features_sample_token.py`（默认值）。前四个是工具脚本，后三个的数据路径都能用命令行参数覆盖（**优先传参，不要改默认值**，便于与源机命令逐字对齐）。

```bash
# 前缀确需替换时（批量改写前先 dry-run 看清单）
grep -rl "/root/autodl-tmp" --include=*.py .
grep -rl "/root/autodl-tmp" --include=*.py . | xargs sed -i 's#/root/autodl-tmp#/data#g'
```

`encoders/lidarclip/lidarclip/model/_mmdet3d_compat.py` 里 `sst/` 的路径由 `__file__` 推导，**不用改**；`CLAUDE.md` 与 `docs/` 里的命令示例属于文档，按需更新即可。

### 6.4 conda 环境

**路线 A：直接搬环境（保真，推荐）**——torch 2.8.0+cu128 / sm_120 的组合已在本机验证，重建容易踩版本坑：

```bash
# 新机
mkdir -p /root/autodl-tmp/.conda-stuff/envs
tar -xf /root/autodl-tmp/transfer/A16_conda_wqlc.tar -C /root/autodl-tmp/.conda-stuff/envs
/root/autodl-tmp/.conda-stuff/envs/wqlc/bin/python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
```

> conda 环境内部有写死的绝对路径（`bin/` 下的 shebang、`conda-meta/` 记录），所以**目标路径必须与源机一致**；路径对不上就只能走路线 B。

环境搬到目标路径后还要让 conda 认到它——源机靠 `/root/.condarc` 注册（该文件在项目外，clone 不会带，**必须手工重建**）：

```bash
# 新机：方式一，直接照抄源机的 .condarc（内容见下）
cat > /root/.condarc <<'EOF'
show_channel_urls: true

envs_dirs:
  - /root/autodl-tmp/.conda-stuff/envs
  - /root/miniconda3/envs

pkgs_dirs:
  - /root/autodl-tmp/.conda-stuff/pkgs
  - /root/miniconda3/pkgs

channels:
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
channel_priority: strict
EOF
conda env list          # 期望能看到 wqlc

# 方式二（等价）：只追加环境目录
conda config --append envs_dirs /root/autodl-tmp/.conda-stuff/envs
```

> 不用 conda 也能直接调环境里的解释器：`/root/autodl-tmp/.conda-stuff/envs/wqlc/bin/python`、`.../bin/pip`，训练脚本里的 `conda run -n wqlc` 按需替换即可。

**路线 B：按清单重建**：

```bash
conda create -n wqlc python=3.10 -y && conda activate wqlc
pip install torch==2.8.0+cu128 torchvision==0.23.0+cu128 --index-url https://download.pytorch.org/whl/cu128

# freeze 里有 16 行 conda 源写法（`包名 @ file:///home/conda/...`）pip 装不了，先剔除
grep -v "@ file://" mllm/env_wqlc_pip_freeze_20260910.txt > /tmp/req_pypi.txt
pip install -r /tmp/req_pypi.txt
# 被剔除的 16 个纯 Python 包单独装（Pillow/httpx/openai 等依赖）
pip install annotated-types anyio certifi distro exceptiongroup h11 h2 hpack \
            httpcore httpx hyperframe jiter openai sniffio typing-inspection typing_extensions

apt-get update && apt-get install -y --no-install-recommends default-jre-headless   # METEOR 需要 java
```

关键版本核对：torch 2.8.0+cu128、torchvision 0.23.0+cu128、transformers 4.47.0、deepspeed 0.16.4、peft 0.13.2、accelerate 1.3.0、mmcv 2.1.0、mmdet 3.2.0、mmengine 0.10.6、pytorch-lightning 2.6.4、nuscenes-devkit 1.1.10、bert-score 0.3.13、nltk 3.10.0、numpy 1.26.4。

**不需要编译任何东西**：环境里没有 spconv，也没有自编译的 CUDA 扩展——`_mmdet3d_compat.py` 用纯 Python 兜底实现了体素化与 scatter，并把 `mmcv.ops` / `mmdet3d.ops` 的扩展模块全部 stub 掉，`sst/` 是 0.15.0 旧版 mmdet3d 源码、靠 `sys.path` 引入。因此**换 GPU 型号不会因架构差异失败**。

### 6.5 其他待补配置

- `OPENAI_API_KEY` / `OPENAI_BASE_URL`（仅数据生成用；`datageneration/config.py` 从环境变量读）
- wandb：离线模式即可（新机无外网时训练照跑，日志落本地）
- 单卡训练沿用 `--include localhost:0`；多卡改 `scripts/*.sh` 里的 deepspeed 参数

---

## 7. 验收清单

```bash
cd /root/autodl-tmp/wql/mmb4dl
git log --oneline -3                                        # 59fed9a 在
ls encoders/lidarclip/b4dl/stage1_features_sample | wc -l   # 期望 28130
ls encoders/lidarclip/b4dl/stage1_features | wc -l          # 期望 29862
ls encoders/lidarclip/b4dl/stage2_features | wc -l          # 期望 850
ls mllm/checkpoints                                         # 期望 stage1 + 3 个 adapter
ls mllm/base_model/vicuna-v1-5-7b                           # 期望 7B 权重齐全
```

```bash
# 编码器 ckpt 可加载，期望 119
conda run -n wqlc python -c "
import torch
ck = torch.load('encoders/lidarclip/ckpt_anneal/lidarclip_mm/last.ckpt', map_location='cpu', weights_only=False)
print(sum(k.startswith('lidar_encoder.') for k in ck['state_dict']))"

# nuScenes 表可加载，期望 nuscenes ok
conda run -n wqlc python -c "
from nuscenes.nuscenes import NuScenes
NuScenes(version='v1.0-trainval', dataroot='/root/autodl-tmp/Datasets/nuScenes', verbose=False); print('nuscenes ok')"
```

最后按 `docs/learn docs/B4DL_B3最优训练方案存档_20260909.md` 里的阶段 2 命令跑一次 B3 评测（`--whole_scene --per_sequence --answer_frames`），mIoU 应回到 0.3467 附近——这一步同时验收了特征、checkpoint、环境三者的对齐。

---

## 8. 坑与注意

1. **小文件必须先打 tar**：`b4dl/` 下 28,130 个 `.npy` 合计仅 333M，逐个 rsync 的元数据往返开销远大于数据本身。
2. **`.ssh/` 绝对不能搬/提交**：仓库内 `.ssh/` 放的是项目私钥（`id_ed25519_b4dl`、`.ssh_project`），已 gitignore。
3. **不要重下 nuScenes**：官方分卷把 samples 与 sweeps 混装（10 个 blob，约 400G），没有"只下关键帧"的选项——31G 子集只能从源机拷。
4. **特征代次不可混用**：`*_once` 是 ONCE 旧编码器产物，与现行（nuScenes 自训 + 退火）不是一代；一旦改动编码器结构、`sst_encoder_only_config.py` 的几何参数或 `loader.py` 预处理，**所有 `.npy` 必须全量重提**（提特征脚本内置"加载不完整即拒绝"的 fail-fast 保护），且训练侧 `mm_projector`（`vtimellm_arch.py:12` 写死 `nn.Linear(768, hidden_size)`）需同步改并重训 stage1。
5. **checkpoint 可瘦身**：`ckpt_anneal/last.ckpt` 1.03G 中编码器只有 34MB；若要另存精简版，务必保留 `{"state_dict": {...}}` 外层包装——加载代码是 `ckpt["state_dict"]` 取键的。
6. **改动先分层**：改 mllm 侧（`dataset.py` 切片、prompt、metatoken 注入、评测规则、RL 奖励）不影响现有特征，旧 adapter 继续可用；改编码器/预处理则旧特征与旧 checkpoint 一起作废。
7. **搬运前确认已推送**：`git status -sb` 若出现本地 commit 未推送，先 `git push`，否则新机 clone 不到最新代码。
8. **迁移完成后再删源机数据**：建议新机验收通过（§7 全部通过 + B3 评测复现）后再清理源机 checkpoints/特征，避免两头落空。
