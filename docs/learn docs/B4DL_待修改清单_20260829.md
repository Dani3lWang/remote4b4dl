# B4DL 待修改清单（2026-08-29，按执行顺序）

> 来源：代码库整体审查（2026-08-29，覆盖编码器训练/特征提取/mllm 训练/数据构建）、
> `B4DL_评测数值对比与问题清单_20260828.md`、`B4DL_论文对齐审计_20260824.md` 遗留项。
> 目标：为下一次重训（B1：编码器定稿 → 全量重提特征 → stage1 162K → mixed 重训）扫清障碍。
> 0824 审计修复项（B1-B6）与 mm_projector 冻结已复查确认在位，不在本清单。

## 2026-08-29 晚间修复进度（commit 3d2e9db/e8639e2/4cd4ada/feceb6e/68afb6f）

来自《B4DL_全仓库论文对齐审查_20260829.md》的 A/B 系列新发现 + 本清单阶段 0-2 代码项：

- ✅ **A1 BERTScore 层号 bug**（24→17）：B0 重算 **0.8973 ≈ 论文 0.897**（原 0.9734 系 bug 虚高）
- ✅ **A3 BLEU-4 切 pycocoevalcap 语料级**：B0 **0.0973 ≈ 论文 0.095**（原 0.1080 句级虚高）
- ⚠️ **A2 METEOR 切 Meteor-1.5 jar**：B0 **0.1729 vs 论文 0.275**——换标准后端后反而显著低于论文，唯一仍未对齐的指标，待考证论文所用 METEOR 变体
- ✅ **A4 GPT 缺失改记 null**（不再伪装成 0.0 分）；实跑仍需 API key（3.2 保持）
- ✅ **A5/A6 requirements 补 nltk/rouge-score/openai；README/CLAUDE.md 依赖表勘误**
- ✅ **B1/B2/B3 datageneration**：TG 生成补全（prompt+dispatch+过滤）、HA 注入接通（scene.json→gt_caption）、任务名别名——**用户已实测 API 链路跑通（2026-08-29），该项闭环**
- ✅ **0.4** seed CLI + save_hyperparameters；**0.6** loader split 字段过滤+700 断言（已验证与旧切片集合一致）
- ✅ **1.1/1.2** 提取脚本 .eval() 定稿 + missing_keys 断言
- ✅ **2.1** 续训按步数排序 + trainer_state.json fail-fast；**2.2** stage1 按步存 mm_projector.bin；**2.3** 特征预校验+失败计数 fail-fast
- 🔄 **0.1/0.5 进行中**：20:25 高 LR 训练判停（step 26,550）→ val MSE 0.1061 → 20:36 启动短程退火（3 epoch / OneCycle max_lr=1e-4→0 / seed 0 / load-only-model，`ckpt_anneal/`），anneal_chain 驱动
- ⏳ 未动：0.2/0.3（监控脚本，v2 已用 CSV 真值，低优先）、1.3 全量重提（待退火完成）、2.4 stage1 构建脚本格式统一、3.1/3.4/3.5、4.x

## P0 —— 阻塞重训质量/安全（按执行阶段排序）

### 阶段 0：编码器续训前（当前编码器已于 08-29 11:50 被判停，step 21,000，lr 仅退火 4%）

| # | 问题 | 位置 | 影响 | 修法 |
|---|---|---|---|---|
| 0.1 | 训练在 OneCycleLR 退火 4% 处被停，0.0759 平台不构成收敛证据 | `encoders/lidarclip/train.py:70-77`；`last.ckpt`（scheduler/optimizer/scaler 完整） | 编码器质量可能低于论文水平；日后返工连累全部特征重提 | 用 `--checkpoint .../last.ckpt` 续训至 lr 实质退火（≥10 epoch）再判停 |
| 0.2 | early_stop_monitor 读到的是"最后进入 top-3 的冻结分"，非实时 loss；判停算术退化为冻结值自比 | `early_stop_monitor.py:54-59` | 平台信号失真、系统性偏向提前停 | 训练端每 50 步 append 实时 loss 到 JSONL，monitor 改读该文件（短期可对 points 去重） |
| 0.3 | MIN_STEP/HARD_CAP 按 batch=32 硬编码 | `early_stop_monitor.py:26-27` | 换 batch 后监控提前误杀训练 | 从 last.ckpt scheduler total_steps 动态推导，或参数化 |
| 0.4 | 无随机种子、ckpt 不存超参（hyper_parameters={}） | `train.py` 全文、`loader.py` | 训练不可复现、事后无法还原配置 | 入口加 `pl.seed_everything()`；`LidarClip` 加 `save_hyperparameters()` |
| 0.5 | OneCycleLR max_lr=1e-3 与 optimizer lr=1e-5 矛盾（1e-5 从未生效，峰值 lr 对 8.5M 编码器偏高） | `train.py:61,70-77` | 平台值虚高、复现误导 | 统一 max_lr 与预期停机点（如 max_lr=1e-4 / max_epochs=5），或续训走完 schedule |
| 0.6 | 测试场景过滤靠 `seq_data[-900:]` 魔法数 + JSON 文件顺序 | `lidarclip/loader.py:104-119` | metadata 重排即静默泄漏 150 个 test 场景（本次实测恰好无泄漏） | 改用现成 `split` 字段过滤 + 断言剩余=700 且与官方 train 集合一致 |

### 阶段 1：编码器定稿后、重提特征前

| # | 问题 | 位置 | 影响 | 修法 |
|---|---|---|---|---|
| 1.1 | 提取不调 `.eval()`：BN 用 batch 统计并更新 running stats、SST 走训练版 token 随机丢弃 | `extract_pc_features.py:45-56`、`extract_pc_features_sample_token.py:45-67` | 特征逐 run 不可复现；未来任何 eval 路径分布跳变 | 明确决策：提特征前 `lidar_encoder.eval()`（与全量重提合并执行），或在 docstring 记录"有意保持 train 模式"，两脚本一致 |
| 1.2 | `load_state_dict(strict=False)` 丢弃返回值，ckpt 与结构不匹配时静默产出垃圾特征 | `extract_pc_features.py:47-48`、`extract_pc_features_sample_token.py:59-60` | 静默失败——特征全错但流程"成功" | 断言 missing_keys 仅含可解释项、unexpected_keys 为空 |
| 1.3 | **现存全部特征都是 ONCE 旧编码器产物**（stage1_features_sample 28,130 帧 08-28 提取时新编码器未开训；stage2_features 850 个为 5 月产物） | `encoders/lidarclip/b4dl/{stage1_features_sample,stage2_features}` | 增量补提会混合两个编码器的特征空间，直接污染 projector 训练 | 新编码器定稿后**全量重提**：28,130 stage1 帧 + 850 stage2 场景（test 场景属推理输出，不算泄漏）；注意 `create_clean_directory` 先删后建无原子性，预留 2h+ 窗口 |

### 阶段 2：mllm 重训前（纯代码修复，约半小时）

| # | 问题 | 位置 | 影响 | 修法 |
|---|---|---|---|---|
| 2.1 | 断点续训三重缺陷：① `train.py` glob 不排序（save_total_limit=3 时大概率恢复最旧 checkpoint）；② 脚本 `--resume_from_checkpoint` 被 train.py 自定义逻辑完全忽略；③ 脚本 `ls -d \| tail -1` 字典序，`checkpoint-900` 会排在 `checkpoint-3400` 之后 | `train.py:444-451`；`run_stage2_full_seqv3_mixed.sh:23-25` | 下一次 21h 混合重训中断后静默丢数百步 / 错误续训 | train.py 按步数数值排序取最大（`max(checkpoints, key=lambda p: int(p.parent.name.split('-')[1]))`）；脚本删 CLI 传参或与 train.py 对齐；`trainer_state.json` 缺失时 fail-fast 而非静默从头训 |
| 2.2 | stage1（projector）训练全程不落 checkpoint | `vtimellm_trainer.py:30-42`（tune_mm_mlp_adapter 直接 pass） | 162K 约 1,266 步零容错，中途 OOM/显存被抢即全部归零 | stage1 也按步保存 `mm_projector.bin`（可从最近一次保存恢复） |
| 2.3 | dataset 异常兜底 `print(e); return random.choice(self)`：无重试上限（极端无限递归）、错误被吞、坏样本被静默随机替换 | `vtimellm/train/dataset.py:446-448` | 训练数据被静默污染或表现为"极慢/卡死" | 启动时预校验全部 scene_id 特征文件存在性（秒级）；运行期失败计数超阈值 fail-fast |
| 2.4 | stage1 构建脚本双版本格式漂移：本地版 `<video>`+scene_id（在用格式）vs official 版 `<image>`+仅 `id` 键（会触发 2.3 的 KeyError 兜底） | `datageneration/tools/build_stage1_from_lidarllm{,.official}.py` | 下次重跑构建脚本可能产出格式漂移数据 | 统一为本地版格式；official 版标注仅参考；`answer_lidar or answer` 兜底加统计打印 |

## P1 —— 强烈建议（不阻塞重训启动）

| # | 问题 | 来源 | 修法 |
|---|---|---|---|
| 3.1 | 推理解码策略未验证（greedy vs beam） | 问题清单 #4 | 1k 样本子集对比 greedy vs beam 3/5，无增益则固化论文口径 |
| 3.2 | GPT-4o 指标缺失 | 问题清单 #7 | predictions.json 已落盘，配 API key 后离线补算（无需重跑推理） |
| 3.3 | 评测 watcher 失败无分类（OOM/超时/挂起不分） | 问题清单 #8 | run_mixed_eval_after_train.sh 失败分支记录错误类型；rc=143 先查 GPU 再等 |
| 3.4 | 编码器提取的 `--use-anno-loader` 路径 batch 解包错位、无效 CLI 参数（--nuscenes-split 等） | 审查 L4 | 删除或接线 |
| 3.5 | wandb offline run 未归档 | 问题清单 #10 | 网络可用时 `wandb sync` |

## P2 —— 可延后/记录即可

| # | 事项 | 说明 |
|---|---|---|
| 4.1 | TG 帧级定位（先验拟合） | 不单独修——待 0.x/1.x/2.x 落地（新编码器+新 projector+混合重训）后以同口径复测，若 mIoU 仍 <0.25 再查解码/metatoken 措辞 |
| 4.2 | 超参扫描 lr∈{5e-5,2e-4} | 待 B1/B2 后 mIoU 仍差再扫（问题清单 #9） |
| 4.3 | 训练场景 699 vs 本地 700 | 已验证零泄漏，复现记录注明即可 |
| 4.4 | tf32 配置差异（stage1 True / mixed 无） | 有意为之（对齐已验证配置），两脚本注释互相说明即可 |
| 4.5 | `run_stage2_full_seqv3.sh` 两阶段驱动器已废弃 | 移 backups 或标注 legacy |
| 4.6 | 评测口径声明（--answer_frames oracle） | 非代码修改：所有报告持续携带声明（问题清单 #5） |

## 已解决（不在清单，存档）

- 问题清单 #3 编码器权重核对 ✅（08-28：本地为 ONCE 权重，nuScenes 自训已启动）
- 问题清单 #6 138 条歧义归属 ✅（08-29：min-width 取最早、两侧一致、影响上限 +0.002，保持现状）
- 0824 审计 B1-B6 修复 ✅（2026-08-29 复查全部在位，无回归）
- stage2 冻结 f_p 疑点 ✅（adapter target_modules 无 mm_projector，冻结成立）

## 执行主线（与基线 B1 计划对齐）

```
阶段0（0.1-0.6）→ 编码器续训至收敛
  → 阶段1（1.1-1.3）→ 全量重提 28,130 + 850 特征
  → 阶段2（2.1-2.4）→ stage1 162K 重训 projector → mixed 重训
  → 同口径评测（冻结命令）→ 对 B0 判定：ΔmIoU > +0.013 才显著
```

## 执行进度（2026-08-30 更新）

| 阶段 | 状态 | 说明 |
|---|---|---|
| 阶段0 | ✅ 完成 | 0.1/0.5 退火链完成（08-30，val MSE 0.1061→0.0992，−6.5%，末段收敛）；0.2 由 LossCSVCallback+v2 监控解决；0.3 随训练结束不再适用；0.4/0.6 commit `4cd4ada` |
| 阶段1 | ✅ 代码完成 / 🔄 执行中 | 1.1/1.2 commit `4cd4ada`（两提取脚本 eval() + strict fail-fast）；1.3 全量重提由 `run_b1_pipeline.sh` 执行中（旧特征已备份 `*_once`） |
| 阶段2 | ✅ 完成 | 2.1/2.2/2.3 commit `e8639e2`；2.4 官方版构建脚本格式对齐（08-30 本轮，scene_id/scene_token 键 + empty_answer 统计） |
| B1 流水线 | 🔄 运行中 | tmux `b1pipeline`：1a/1b 重提 → stage1 162K 重训 → mixed-b1（独立 output_dir，B0 产物原封不动，95K projector 已备份 .bak_95k）→ 同口径评测。每小时 cron 报告 |
| B0 口径复测 | ✅ | commit `0002e0e`：BERTScore 0.8973 / BLEU-4 0.0973 换对后端后命中论文 0.897/0.095；METEOR jar 后端 0.1729 为唯一遗留口径考证项 |

编码器定稿：`ckpt_anneal/lidarclip_mm/last.ckpt`（退火 3ep，1e-4→0，seed 0）。
