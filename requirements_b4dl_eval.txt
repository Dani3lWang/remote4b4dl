# =============================================================================
# B4DL 评估扩展 — 密集视频描述 / 时序定位指标
# =============================================================================
# 环节: mllm/vtimellm/eval/
# 依赖: 需先安装 requirements_b4dl.txt
# =============================================================================

# ---- Captioning 指标 (METEOR / CIDEr / BLEU / Rouge-L) ----
# 需要系统安装 Java 1.8+:
#   conda install -c conda-forge openjdk=11
pycocoevalcap==1.2

# ---- BERT-based 指标 (SODA_c) ----
bert_score==0.3.13
moverscore==1.0.3
