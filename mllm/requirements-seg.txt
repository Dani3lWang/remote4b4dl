# Install in a dedicated environment based on the verified B3 runtime.
torch==2.8.0
transformers==4.47.0
peft==0.13.2
accelerate==1.3.0
deepspeed==0.16.4
safetensors>=0.4.5
numpy>=2.0
nuscenes-devkit==1.1.10
# CUDA 12.x wheel; verify import and a sparse forward pass on the target GPU.
spconv-cu120>=2.3.6
